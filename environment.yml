name: pytask-r
channels:
  - conda-forge
  - r
  - pytask
dependencies:
  - python
  - pip

  # Conda
  - anaconda-client
  - conda-build
  - conda-verify

  # Package dependencies
  - pytask >= 0.0.5
  - r-base

  # Misc
  - bumpversion
  - jupyterlab
  - pdbpp
  - pre-commit
  - pytest-cov
  - pytest-xdist
