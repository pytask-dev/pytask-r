name: pytask-r

channels:
  - conda-forge
  - r
  - pytask

dependencies:
  - python
  - pip
  - setuptools-scm
  - toml

  # Conda
  - anaconda-client
  - conda-build
  - conda-verify

  # Package dependencies
  - pytask >=0.0.9
  - pytask-parallel >=0.0.4
  - r-base

  # Misc
  - bumpversion
  - jupyterlab
  - pdbpp
  - pre-commit
  - pytest-cov
  - pytest-xdist
  - versioneer
  - black
