[tox]
envlist = pytest

[testenv]
package = editable

[testenv:test]
extras = test
conda_channels =
    conda-forge
    nodefaults
conda_deps =
    r-base >=4
    r-jsonlite
    r-yaml
commands =
    pytest {posargs}
